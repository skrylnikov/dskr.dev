name: Build Astro site

on:
  push:
    branches: [master]
    paths:
      - src/**
      - public/**
      - astro.config.mjs
      - package.json
      - pnpm-lock.yaml
      - tsconfig.json
      - Dockerfile
      - .dockerignore
      - .github/workflows/site.yml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        env:
          IMAGE: ghcr.io/skrylnikov/dskr.dev
          SHA: ${{ github.sha }}
        run: |
          tag="master-$(date +%s)-${SHA}"
          docker buildx build --file Dockerfile --tag "${IMAGE}:${tag}" --push .
