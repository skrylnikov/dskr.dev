---
import BaseHead from '../components/BaseHead.astro';
import BlogHeader from '../components/BlogHeader.astro';
import { L } from '../i18n/i18n-node';
import type { Props as BaseHeadProps } from '../components/BaseHead.astro';

export interface Props extends BaseHeadProps {}

const { title, description, image, imageAlt, type, noindex, publishedTime, modifiedTime, alternates, structuredData } = Astro.props;
const LL = L[Astro.currentLocale];
const rssUrl = Astro.currentLocale === 'en' ? '/en/rss.xml' : '/rss.xml';
---

<html lang={Astro.currentLocale || 'ru'}>
	<head>
		<BaseHead {title} {description} {image} {imageAlt} {type} {noindex} {publishedTime} {modifiedTime} {alternates} {structuredData} />
	</head>
	<body>
		<a class="skip-link" href="#main-content">{LL.common.skipToContent()}</a>
		<BlogHeader />
		<main id="main-content" class="page-shell" transition:animate="fade">
			<slot />
		</main>
		<footer class="site-footer shell">
			<span>© {new Date().getFullYear()} Dmitry Skrylnikov</span>
			<div class="footer-links">
				<a href={rssUrl}>RSS</a>
				<a href="https://github.com/skrylnikov" target="_blank" rel="noreferrer">GitHub</a>
			</div>
		</footer>
	</body>
</html>
